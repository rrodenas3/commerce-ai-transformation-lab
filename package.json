{
  "name": "commerce-ai-transformation-lab-evidence-explorer",
  "version": "0.1.0",
  "private": true,
  "description": "Browser verification for the public, read-only Stage 2 evidence explorer.",
  "scripts": {
    "serve:demo": "node tests/browser/server.js",
    "test:browser": "playwright test",
    "test:browser:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
